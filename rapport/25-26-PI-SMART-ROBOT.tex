\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage{amsmath}
\usepackage{listings}
\usepackage{xcolor}
\usepackage[margin=2.5cm]{geometry}
\usepackage{graphicx}
\usepackage{fancyhdr}
\usepackage{hyperref}
\usepackage{tabularx}
\usepackage{array}
\usepackage{float}

%Nécessaire pour l'affichage des codes C.
\definecolor{mygreen}{rgb}{0,0.6,0}
\definecolor{mygray}{rgb}{0.5,0.5,0.5}
\definecolor{mymauve}{rgb}{0.58,0,0.82}
\definecolor{myblue}{rgb}{0.13,0.29,0.53}
\definecolor{mybackground}{rgb}{0.96,0.98,0.98}

\lstdefinestyle{modernC}{
    backgroundcolor=\color{mybackground},
    commentstyle=\color{mygreen}\itshape,
    keywordstyle=\color{myblue}\bfseries,
    numberstyle=\tiny\color{mygray},
    stringstyle=\color{mymauve},
    basicstyle=\ttfamily\small,
    breakatwhitespace=false,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=8pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=4,
    frame=single,
    rulecolor=\color{black!30},
    language=C
}

\lstset{style=modernC}


\usepackage[french,ruled,lined,linesnumbered,algochapter]{algorithm2e}


\lstset{
    basicstyle=\ttfamily\small,
    breaklines=true,
    frame=single,
    numbers=left,
    numberstyle=\tiny,
    stringstyle=\color{orange},
    keywordstyle=\bfseries\color{blue},
    commentstyle=\color{green!60!black},
}

\begin{document}

\begin{titlepage}
    \centering
    
    \vspace*{2cm}
    
    \includegraphics[width=0.3\textwidth]{logo_insa_new.png}\\[1cm]
    
    {\large INSA ROUEN NORMANDIE}\\[0.2cm]
    {\normalsize Département Informatique}\\[2cm]
    
    % Ligne séparatrice
    \rule{0.8\textwidth}{0.5pt}\\[0.5cm]
    
    % Titre
    {\huge\bfseries PROJET INTÉGRATIF}\\[0.5cm]
    {\Huge\bfseries SMART ROBOT}\\[0.3cm]
    {\Large Robot Autonome}\\[0.5cm]
    
    \rule{0.8\textwidth}{0.5pt}\\[2cm]
    
    % Équipe
    {\large\bfseries Équipe projet :}\\[0.5cm]
    \begin{tabular}{c}
        Delacroix-Henrion Mathieu\\
        Diallo Alioune\\
        Malherbe Ylann\\
        Mathieu Ilan\\
        Olivier Neil\\
        Schetrit Jahnis
    \end{tabular}
    
    \vfill
    
    % Informations en bas
    {\large ITI3 --- Année 2025-2026}\\[0.3cm]
    {\Large\bfseries Décembre 2025}
    
\end{titlepage}


\newpage
\tableofcontents
\newpage
\section{Introduction}
\subsection{Contexte}


Dans le cadre de notre premier semestre dans le départment ISIA, nous avons pour but de réaliser un \textit{Smart Robot} capable de se déplacer dans une ville modélisée sous forme de cases.
Equipé de capteurs, moteurs et d'une raspberry Pi 4, notre objectif est de construire et coder un robot capable de parcourir un itinéraire imposé tout en determinant le plus court chemin permettant de passer par un certain nombre de points obligatoires.
C'est en combinant électronique, algorithmique et développement en C, ainsi que l'utilisation de GitLab que nous allons réépondre aux attentes de ce projet.
Ce rapport est structuré de la manière suivante:

Dans un premier temps, nous présenterons la partie électronique contenant la description de la conception du robot avec le schéma électrique, les Choix techniques que nous avons effectués, ainsi que les branchements et problèmes que nous avons pu rencontrés.\\
Dans un deuxième temps, nous présenterons la partie informatique, depuis l'analyse descendante du problème, jusqu'à l'implémentation des fonctions et procédures en C.
Enfin, nous conclurons avec la répartition des tâches et ce que l'on a pu tirer de ce projet.

\subsection{Problematique}
La problématique principale de ce projet est de faire en sorte que le robot parcours la ville de manière autonome et optimale en interprétant correctement les descriptions de la ville.
En effet, le robot doit lire et traduire ces informations en une suite d'ordre (AV, TG, TD) lui permettant de se déplacer dans la ville. 
Celui-ci doit non seulement suivre les lignes au sol en faisant attention à corriger d'éventuelles déviations, mais aussi detecter et signaler les pastilles colorées signalant les points de passages obligatoires.
En définitive, notre robot doit être capable de réaliser plusieurs actions en parralèle de manière autonome.

\section{Partie électronique}

\subsection{Cahier des charges}

Pour la partie électronique, le cahier des charges est assez clair et précis car il était renseigné dans le sujet. 
La partie électronique se concentre sur le robot, ses composants et son fonctionnement. Les attendus sont les suivants : 

Un robot qui suit un chemin tracé au sol, passant par des points précis, et qui revient à son point de départ. 
Lorsque le robot passera sur le point distinguable par une pastille de couleur, il emettra un son distinct et affichera la couleur de la pastille sur le LCD. 
Chacune de ses actions doivent être affichée en temps réel sur l'écran du LCD (Avancer, Virage à Gauche et Virage à Droite). 
L'arrivée qui est également le point de départ sera visible et détectable par le robot car il sera representé par une pastille rouge.

Notre objectif va alors être de mettre en place les composants nécessaires ainsi que coder les algorithmes indispensables afin de répondre à ce cahier des charges.

\subsection{Choix des composants}

Afin de réaliser notre robot, nous avions plusieurs composants à choisir tant sur le nombre de composants à intégrer dans la construction de notre robot, que sur l'emplacement dans le robot.
En ce qui concerne les moteurs, ceux-ci sont alimentés à l'aide de piles connectées en parallèles et sont reliés à un interrupteur.
La raspberry Pi est alimentée par une batterie externe afin de garantir une autonomie et une alimentation stable.
Le buzzer et les capteurs (suiveurs de ligne et RGB) sont connectés aux GPIO de la RaspberryPi et alimentés avec 3,3V.
Concernant l'écran LCD, celui-ci nécessite une alimentation de 5V comme précisé dans sa documentation technique.
Avant de monter intégralement les composants sur le robot, nous avons testés chacun des composants séparéments:
\begin{itemize}
    \item Une unité de test pour les moteurs (Avancer, reculer, s'arrêter...)
    \item Une unité de test pour les capteurs (détecter une surface sombre, détecter une surface claire, détecter une pastille de couleur...)
    \item Une unité de test pour l'affichage sur l'écran LCD (afficher 'Bonjour', effacer l'écran, afficher la couleur détectée par le capteur RGB...)
\end{itemize}
Ces test nous ont permis d'ajuster l'emplacement des composants, la calibration des capteurs, de la même manière que trouver plus facilement les composants possiblement déeffectueux.

\subsubsection{Capteur suiveur de ligne}
\textbf{Rôle}

Les capteurs suiveurs de ligne permettent au robot de détecter et donc de suivre une ligne noire tracée sur une surface plus claire.
L'objectif est de guider les mouvements du robot, de tel sorte que celui-ci corrige sa direction s'il n'est pas bien aligné sur la trajectoire de la ligne.
En incluant ces types de capteurs dans notre projet, nous sommes capable de garder le robot sur la ligne noir, de détecter lorsque celui-ci dévie de la trajectroie, mais cela permet 
aussi de repérer les intersections et virages lui permettant de s'orienter.

\textbf{Fonctionnement}

Ces types de capteurs fonctionnent de la manière suivante:\\
Le noir absorbe la lumière tandis que le blanc ou les couleurs plus claires la réflechisse.
Les capteurs de ligne sont alors composés d'une LED infrarouge qui éclaire le sol en continu et d'une photodiode mesurant la lumière renvoyée.
Concrètement, si un capteur suiveur de ligne se trouve sur une surface claire, alors la lumière infrarouge est fortement réflechie.
Le capteur reçoit donc beaucoup de lumière et envoie le signal iniquant qu'il s'agit d'une surface claire.
Au contraire, sur une surface noire, la lumière est absorbée donc le capteur ne reçoit que très peu de lumière.
Celui-ci envoie alors le signal indiquant qu'il s'agit d'une surface noire.

\subsubsection{Capteur RGB}
\textbf{Rôle}

Le capteur de couleur TCS 34725 permet au robot de detecter la couleur des pastilles présentes sur le parcours comme la pastille rouge indiquant le point de départ du robot.
Ce capteur est très important car il permet d'identifier correctement les points de passage, mais aussi d'afficher la couleur que ce capteur détecte sur l'écran LCD.
Il est essentiel d'inclure ce capteur, sans quoi, notre robot ne serait pas capable de repérer les points de passages obligatoires du parcours repéré par une pastille de couleur verte.

\textbf{Fonctionnement}

Le capteur de RGB, comme son nom l'indique, permet de repérer trois couleurs: rouge, vert et bleu.
Le TSC 34725 possède une petite LED blanche pour éclairer la surface et alors mesurer l'intensité lumineuse pour chacune des composantes (composante rouge, bleue ...).
Ensuite, les valeurs sont converties en valeurs numériques via le moyen de communication I2C qui sont ensuite lus par la carte embarquée de la Raspberry Pi 4.
Les trois intensités sont alors combinées pour determiner de quelle couleur il s'agit.

\subsubsection{Moteur Driver}
\textbf{Rôle}

Le moteur driver est un intermédiaire entre la Raspberry Pi 4 et les moteurs du robot car il permet de fournir assez de courant aux moteurs, régler la vitesse, contrôler le sens de rotation des roues mais aussi de protéger la raspberry car celle ci ne peux pas alimenter directement les moteurs.
En effet, la Raspberry ne fournirait que 3.3V ou au maximum 5V pour les moteurs tandis que ceux-ci peuvent être alimenter jusqu’à 12V. Il est donc indispensable d’utiliser un moteur Driver.

\textbf{Fonctionnement}

Un moteur Driver contient 2 canaux moteurs (moteur 1, moteur 2) ainsi que 6 broches de contrôle:
\begin{itemize}
    \item Enable1: permet de régler la vitesse du moteur 1
    \item Enable2: permet de régler la vitesse du moteur 2
    \item IN1, IN2: permettent de contrôler le sens du moteur 1
    \item IN2, IN3: permettent de contrôler le sens du moteur 2
\end{itemize}
Afin de contrôler correctement le moteur, il est important de savoir comment fonctionnent ces différentes broches. 
Tout d'abord, pour contrôler la vitesse d'un moteur, nous avons besoin du PWM (Pulse Width Modulation) qui permet de simuler une tension variable à partir d'un signal (HIGH ou LOW). 
Lorsque l'on met cette valeur à HIGH(1), le moteur est activé à pleine vitesse. 
Inversement, lorsque l'on met cette valeur à LOW(0), le moteur est désactivé.
Enfin, en ce qui concerne les broches permettant de contrôler le sens du moteur, celles-ci fonctionnent comme suit:

\begin{table}[htbp]
\centering
\renewcommand{\arraystretch}{1.5}
\small
\begin{tabular}{|l|c|c|}
\hline
\textbf{IN1} & \textbf{ IN2} & \textbf{Action}  \\ \hline
0 & 0 & rotation dans le sens 1\\ \hline
0 & 1 & rotation dans le sens 2\\ \hline
1 & 0 & arrêt \\ \hline
1 & 1 & frein \\ \hline
\end{tabular}
\caption{Fonctionnement des broches}
\end{table}

\subsubsection{Ecran LCD}
\textbf{Rôle}

Un écran LCD I2C permet d’afficher les informations provenant du robot comme les indications concernant son trajet sur le parcours (Tourner gauche, intersection…)
Mais aussi d’afficher la couleur des pastilles situées sur les points de passages obligatoires.
Concrètement, il s’agit de l’interface entre le robot et nous, utilisateurs.

\textbf{Fonctionnement}

Un écran LCD utilise beaucoup de fils, pour gérer cela, nous utilisons un LCD avec l’I2C pour réduire à seulement 2 fils de communication. Le câblage est alors plus simple et cette configuration est idéale pour notre robot surtout quand on utilise déjà beaucoup de GPIO avec les capteurs et moteurs. 

Seulement 4 lignes sont utilisées: 
\begin{itemize}
    \item SDA: données
    \item SCL: horloge
    \item VCC: alimentation
    \item GND: masse
\end{itemize}
Il est intéressant de noter que tous les périphériques I2C sont connectés en parallèle sur SDA/SCL. C’est exactement ce que l’on a pu faire avec le capteur RGB.
L’écran LCD fonctionne de la manière suivante:

Le micro contrôleur envoie les commandes comme le texte à afficher ou encore effacer l’écran. Le module I2C lui, reçoit les données et les converti en signaux compréhensibles par le LCD.
\newpage
\subsubsection{Buzzer}
\textbf{Rôle}

Le Buzzer a servi d'emmeteur de son lorsque le robot détecte sur une pastille. 
Concrètement il s'agit d'un moyen pour le robot de nous confirmer qu'il a bien repéré la pastille.

\textbf{Fonctionnement}

Le buzzer est un composant assez simple qui ne demande pas de protocole de communication particulier. Pour le gérer, nous utilisons directement un GPIO de la Raspberry Pi 4. Le câblage est rapide et cette configuration est pratique car Seulement 3 lignes sont utilisées sur le module : 
\begin{itemize}
    \item S : Signal de commande
    \item VCC: L'alimentation
    \item GND : La masse
\end{itemize}

\subsection{Conception du Robot}
\subsubsection{Schéma électrique}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{schema_electrique.jpg}
    \caption{Schéma électrique du robot autonome}

    % Légende des couleurs (plus discrète)
    \vspace{8pt}
    \small
    \fcolorbox{black!10}{white}{\begin{minipage}{0.7\textwidth}
        \small
        \textbf{Légende des couleurs} \vspace{4pt}
        \begin{tabular}{@{}m{1.2cm} m{4.5cm}@{}}
            \textcolor{red!60}{\rule{1cm}{0.15cm}} & Fil d'alimentation (3.3V ou 5V) \\
            \textcolor{black!80}{\rule{1cm}{0.15cm}} & Fil de masse (GND) \\
            \textcolor{green!60}{\rule{1cm}{0.15cm}} & Communication I2C (SDA et SCL) \\
            \textcolor{orange!60}{\rule{1cm}{0.15cm}} & GPIO des capteurs \\
            \textcolor{brown!60}{\rule{1cm}{0.15cm}} & Écran LCD \\
            \textcolor{pink!60}{\rule{1cm}{0.15cm}} & Fil de commande moteurs \\
            \textcolor{violet!60}{\rule{1cm}{0.15cm}} & Fil moteur shield \\
        \end{tabular}
    \end{minipage}}
    \label{fig:schema}
\end{figure}


\subsubsection{Choix techniques}

Pour le montage du robot, nous avons commencé par nous rassembler et discuter des différents composants que nous pouvions inclure au robot.
Tout d'abord, nous avons discuté du meilleur emplacement pour les capteurs de contrastes, essentiels au projet pour détecter les lignes noires du parcours.
Afin de suivre au mieux la ligne, trois possibilités s'offraient à nous :
\begin{itemize}
    \item Deux capteurs reprerant la ligne noire devant
    \item Deux capteurs de part et d'autre de la ligne noire devant
    \item Trois capteurs, un reperant la ligne noire et deux capteurs detectant une surface autre que noire 
\end{itemize}
La première option nous semblait au début la meilleure, mais lors des tests nous avons remarqué que les deux capteurs quittaient vite la ligne noir et le robot était alors "perdu". L'option à trois capteurs nous semblait très fortement similaire à la deuxième car la correction de trajectoire s'effectuait à l'aide des capteurs autour de la ligne. 
Ces raisons nous ont donc poussé à choisir la solution avec deux capteurs autour de la ligne renvoyant l'information de la déviation permettant au programme de la corriger. 
Les tests furent après cela, très concluant.

De plus, nous avons choisi de placer deux capteurs suiveurs de lignes sur les cotés, un capteur sur le côté droit, ainsi qu'un capteur sur le côté gauche afin de détecter les intersections et virages. 
Les capteurs se situent assez proche des roues, ce qui signifie que le robot se retrouvera au centre de l'intersection lorsque celui-ci devra effectuer un virage. 
Pour cette raison, nous avons choisi d'implémenter un virage sur place plutôt que en avançant. 
Mais encore, le virage s'arrête lorsque le capteur de ligne (et non d'intersection) externe au virage retrouve une ligne noir.
Cela est alors possible puisque nous avons choisi d'interdire les demis-tours.

En ce qui concerne le capteur de couleur, étant donné que les pastilles étaient au sol, nous l'avons logiquement positionné au plus près, en dessous du robot.

Enfin nous avons calibré chaque composant pour que le rendu soit le plus propre possible et le plus près du sujet.
Pour ce qui est de la partie moteur, le courant est contrôlé par un interrupteur et le reste pas un "Moteur Driver". 
Lors du parcours le robot devra bipper (avec notre buzzer) lorsqu'il rencontra une pastille. et s'il croise la pastille rouge alors il devra s'arrêter.

A la suite de tous ces choix, notre robot a alors pu être construit et décoré, prêt à être utilisé.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{megatron.JPG}
    \caption{smart robot personnalisé}
    \label{fig:monimage}
\end{figure}


\subsubsection{Estimation du robot}

Afin d'estimer les différentes valeurs de puissances consommées par notre robot nous sommes allée chercher les valeurs dans les datasheets associées au composants sur leurs différents site de vente ou sur le site du producteur. 
Si certaines datasheet sont indisponibles ou introuvables, alors,  nous avons pris la décision de choisir une valeur moyenne des produits similaires que nous pouvons trouver.  

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.5}
\small
\begin{tabular}{|l|c|c|c|}
\hline
\textbf{Appareil} & \textbf{Tension (V)} & \textbf{Courant (mA)} & \textbf{Puissance (W)} \\ \hline
TCS34725 & 3,3  & 0,33 & $1,09 \times 10^{-3}$ \\ \hline
Capteur de ligne x4 & 3,3  & 20  & 0,264  \\ \hline
L293D x2 & 3,3  & 600  & 3,96  \\ \hline
LCD1602 & 5  & 21  & 0,105  \\ \hline
Buzzer actif & 3,3  & 30  & 0,099  \\ \hline
PCF8574 & 3,3  & 0,04  & $0,132 \times 10^{-3}$ \\ \hline
Raspberry Pi & 5  & 1 200 & 6  \\ \hline
\end{tabular}
\caption{Bilan de consommation du robot }
\end{table}


Ainsi pour estimer l'autonomie de notre batterie nous allons simplement venir diviser la capacité de celle-ci par le courant total consommé par les différents composants.

\vspace{2mm}
Les composants liés a la batterie sont:
\begin{itemize}
    \item Le buzzer actif
    \item Les 4 capteurs suiveurs de lignes
    \item Le capteur RGB
    \item Les 2 contrôleur de pilote du moteur
    \item L'écran LCD
    \item La Raspberry Pi 4
\end{itemize}
Ainsi la consommations estimée des composants est d'environ 10.4 W, dont 4.32 W pour les éléments alimentés en 3,3 V et 6.11 W pour ceux alimentés en 5 V.

\vspace{1mm}

Il est important de noter que les moteurs, étant alimentés séparément par des piles, leur consommation n’est pas incluse dans ce calcul.

\vspace{2mm}

L’autonomie du système peut donc être estimée à partir de l’énergie fournie par la batterie.
Elle est donnée par le rapport entre l’énergie stockée dans la batterie et la puissance consommée par le système (environ 10.4 W hors moteurs).
Ainsi, pour une batterie d’énergie 37.0 (en Wh), l’autonomie est approximativement T = 37.0 / 10.4 = 3h33 min. En tenant compte des pertes liées aux convertisseurs de tension, l’autonomie réelle est comprise entre 80 \% et 90 \% de cette valeur théorique.


\subsection{Difficultés rencontrées et pistes d'amélioration}

Nous avons rencontré de nombreuses difficultés, aussi bien sur le plan électronique que sur le plan organisationnel.\\

Tout d'abord, pour ce qui était de l'électronique, les premiers problèmes furent de s'accoutumer avec les composants. 
Pour la majorité des composants, on ne les avait jamais vu avant et comprendre leur fonctionnement nécessitait d'avoir un code lié et de pouvoir faire des tests. Or nous n'avions pas de code pré-disposé.
La suite fut de passer de la théorie à la pratique. 
En effet, lorsque nous avons codé le comportement du robot, nous nous sommes rendu compte de la différence entre notre comportement théorique et le fonctionnement réel. 
Une erreur dans la récupération de l'information des capteurs nous empêchait d'avancer. 

Un autre problème qui nous a beaucoup occupé fut la détection de couleur. 
Lors de nos tests préliminaires le capteur fonctionnait comme souhaité, mais en pratique non, et notre manière de trouver les bonnes valeurs pour le faire fonctionner restait assez hasardeuse.

De plus, faire un montage stable pour le robot fut une réelle épreuve. 
Certains câbles ne se branchaient pas correctement et/ou se détachaient lorsque nous faisions rouler le robot, nous faisant passer parfois beaucoup de temps à reperer le problème. 
Faire tenir les composants sur la structure du robot n'était pas de tout repos non plus. 
Il était difficile de monter le robot de manière stable dûe à ses dimensions assez petites.\\

De l'autre coté nous avons aussi eu des difficultés d'organisation. 
Au début, nous avions séparé le groupe en deux. Mais très vite, nous avons remarqué que nous perdions du temps et de l'efficacité. 
Alors nous avons choisi de faire trois groupe de deux ce qui nous a permi de prêter plus de temps à la partie algorithmique tout en restant efficace sur la partie électronique.\\

Il va sans dire que notre robot reste largement améliorable tant dans notre gestion des tâches et de l'effectif que dans notre relecture du code et notre vigilance sur ce que l'on écrit. 
La détection de couleur pourrait être plus stable et efficace. 
Après de nombreux essais, le robot échoue parfois à accomplir sa tâche comme nous avons pu en faire les frais lors de la soutenance. Seulement 2 pastilles sur les 3 ont été détectée. 
Cela est du à plusieurs paramètres comme l'éclairage de la pièce, ou encore la couleur du circuit.

\subsection{Logigramme de la réponse au cahier des charges}

A la suite de toutes ces explications, choix, et prises de décisons, nous avons pu répondre au cahier des charges imposé.
Afin d'illustrer cela, nous avons réalisé un Logigramme du robot reprenant chacune des partie du cahier des charges et la réponse par laquelle nous avons répondu à celui-ci.

\begin{figure}[H]
    \centering
    \includegraphics[width=17cm]{LogigrammeRobot.png}
    \caption{Logigramme du robot}
    \label{fig:monimage}
\end{figure}

\section{Partie informatique}

\subsection{Cycle en V}
\subsubsection{Analyse}

L'analyse a un rôle essentiel dans ce projet afin d'identifier les principaux problèmes causés par celui-ci, et alors de traiter ces problèmes en sous problèmes plus simples à résoudre.

Les principaux problèmes à resoudres sont:
\begin{itemize}
    \item Étudier le fichier \textbf{.txt} afin de récuperer les informations essentielles liées au robot et à la construction de la ville
    \item Créer un graphe modélisant les liaisons entre les différents points de la ville
    \item Calculer le chemin le plus court
    \item Génerer les ordres que le robot doit suivre sur la sortie standard
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[
        width=0.9\textwidth, % Ajuste la largeur de l'image à 90% de la largeur du texte
        keepaspectratio     % Garde les proportions de l'image
    ]{PI - Projet Robot-Aanlyse_descendante_partie_algo.pdf}
    \caption[Analyse descendante de la partie algorithme]{%
        \textbf{Analyse descendante de la partie algorithme.}
    }
    \label{fig:Analyse_descendante_partie_algo}
\end{figure}

\paragraph{A. Choix de l'algorithme du plus court chemin }\mbox{} \\[5pt] 
Étant donné que nous avons étudié en cours certains algorithmes de parcours, il a fallu choisir celui qui serait le plus adapté pour notre projet.
Souhaitant représenter la ville sous forme de graphe, nous avions le choix entre deux algorithmes bien précis:
\begin{itemize}
    \item \textbf{Dijkstra} : Un algorithme efficace pour trouver le plus court chemin dans un graphe pondéré.
    \item \textbf{A*} : Une version améliorée de Dijkstra, utilisant une heuristique pour optimiser la recherche.
\end{itemize}
Étant donné la petite taille du labyrinthe, nous avons opté pour un Dijkstra dont les distances entre sommets des graphes seraient tous égaux à un.
\newpage
\paragraph{B. Types} \mbox{} \\[5pt] 
En réalisant cette analyse descendante, nous nous sommes rendus compte que nous aurions besoin de plusieurs types afin de mener à bien l'algorithme souhaité.
Voici les Types que nous souhaitons mettre en place par la suite:\\

\textbf{Type Direction}:
Ce type permet d'associer une direction(Sud, Nord, Ouest, Est) au robot et permet donc de faciliter l'interdiction des demi-tours quant à la direction actuelle du robot.\\

\textbf{Type Ordre}: 
Ce type est une énumération qui permet de donner au robot les actions qu'il peut effectuer tout au long du parcours (Avancer, Tourner Droite, Tourner Gauche).\\

\textbf{Type Robot}: 
Ce type permettra de regouper les informations du robot comme la case ou il se trouve et sa direction.\\

\textbf{Type Chemin}: 
Ce type permettra de contenir l'ensemble des points par lequel le robot doit passer pour réaliser le chemin le plus court.\\

\textbf{Type EtatDuDijkstra}: 
Ce type permettra de renseigner le point actuel ou l'on se trouve, le point depuis lequel on arrive (point précédent) ainsi que le chemin pour parvenir jusqu'au point actuel.\\

\textbf{Type Solution}: 
Ce type sera utilisé lors de la résolution du chemin le plus court, regroupant alors l'ordre dans lequel nous allons passer par les points obligatoires ainsi que le chemin complet des points par lesquels nous allons passer.\\

\textbf{Type File}: 
Le dernier type est essentiel car nous avons choisi d'utiliser des files pour l'algorithme de Dijkstra. Cette File stockera les différents états du Dijkstra que l'on pourra obtenir grâce aux indice de début et fin de la file.\\

Pour ce qui est du Graphe, nous utilisons les TAD déjà implémentés de Monsieur Delestre utilisant de la même manière les TAD ListeChaineeListe ainsi que tableHachage.

\subsubsection{Conception préliminaire}

\paragraph{A. TAD} \mbox{} \\[5pt] 
\textbf{TAD tDirection}\\
\noindent\textbf{Nom} : tDirection

\noindent\textbf{Opérations} :
\begin{itemize}
    \item \textbf{directionOuest} : tDirection $\rightarrow$ tDirection
    \item \textbf{directionEst} : tDirection $\rightarrow$ tDirection
\end{itemize}

\vspace{\baselineskip}

\textbf{TAD Robot}\\
\noindent\textbf{Nom} : Robot

\noindent\textbf{Utilise} :
\begin{itemize}
    \item tDirection
    \item NaturelNonNul
\end{itemize}
\noindent\textbf{Opérations} :
\begin{itemize}
    \item \textbf{setDirection} : tDirection $\times$ Robot $\rightarrow$ Robot
    \item \textbf{getDirection} : Robot $\rightarrow$ tDirection
    \item \textbf{getCaseRobot} : Robot $\rightarrow$ NaturelNonNul
    \item \textbf{setCaseRobot} : Robot $\times$ NaturelNonNul $\rightarrow$ Robot
\end{itemize}

\vspace{\baselineskip}

\textbf{TAD Chemin}\\
\noindent\textbf{Nom} : Chemin

\noindent\textbf{Utilise} :
\begin{itemize}
    \item NaturelNonNul
\end{itemize}
\noindent\textbf{Opérations} :
\begin{itemize}
    \item \textbf{obtenirChemin} : Chemin $\rightarrow$ tableau[1.. NB\_POINTS\_MAX] de NaturelNonNul 
    \item \textbf{obtenirNbPoints} : Chemin $\rightarrow$ NaturelNonNul
    \item \textbf{fixerNbPoints} : Chemin $\times$ NaturelNonNul $\rightarrow$ Chemin
\end{itemize}

\vspace{\baselineskip}

\textbf{TAD File} \\
\noindent\textbf{Nom} : File

\noindent\textbf{Utilise} :
\begin{itemize}
    \item EtatDuDijkstra
    \item Naturel
\end{itemize}
\noindent\textbf{Opérations} :
\begin{itemize}
    \item \textbf{initialisation\_file} :$\rightarrow$ File 
    \item \textbf{estVideFile} : File $\rightarrow$ Naturel
    \item \textbf{estPleineFile} :  File $\rightarrow$ Naturel
    \item \textbf{enfiler} : File $\times$ EtatDuDijkstra $\rightarrow$ File
    \item \textbf{defiler} : File $\rightarrow$ File $\times$ EtatDuDijkstra
    \item \textbf{liberer\_file} : File $\rightarrow$
\end{itemize}
\noindent\textbf{Préconditions} :
enfiler$(f, pDij)$ : non estPleineFile$(f)$

\noindent\textbf{Préconditions} :
defiler$(f)$ : non estVideFile$(f)$

\paragraph{2. Signatures des fonctions}\mbox{} \\[5pt] 
Dans cette sous partie, nous établirons les signatures de toutes les fonctions et procédures utilisées dans la partie algorithme du projet.
Toutefois, pour la partie conception détaillée, nous ne présenterons que les algorithmes non triviaux et essentiels à notre résolution du problème principal: Trouver le chemin le plus court.\\

\textbf{Fonctions et procédures du TAD Robot}
\begin{itemize}
    \item\texttt{Fonction directionOuest(direction: tDirection) : tDirection}
    \newline
    \item\texttt{Fonction directionEst(direction: tDirection) : tDirection}
    \newline
    \item\texttt{Fonction getDirection(r: Robot) : tDirection}
    \newline
    \item\texttt{Procedure setDirection(E/S r: Robot, E direction: tDirection)}
    \newline
    \item\texttt{Fonction getCaseRobot(r: Robot) : NaturelNonNul}
    \newline
    \item\texttt{Fonction setCaseRobot(E/S r: Robot, E nouvelleCase: NaturelNonNul)}
\end{itemize}

\vspace{1.1em}

\textbf{Fonctions et procédures du TAD Chemin}
\begin{itemize}
    \item\texttt{Fonction obtenirNbPoints(c: Chemin) : naturel}
    \newline
    \item\texttt{Procedure obtenirChemin(E c: Chemin, S tabPoints: tableau[1..NB\_POINTS\_MAX] de NaturelNonNul)}
    \newline
    \item\texttt{Procedure fixerNbPoints(E/S c: Chemin, E nombre\_points: NaturelNonNul)}
\end{itemize}

\vspace{1.1em}

\textbf{Fonctions et procédures du TAD File}
\begin{itemize}
    \item\texttt{Fonction initialisation\_file(): File}
    \newline
    \item\texttt{Fonction estVideFile(f: File) : naturel}
    \newline
    \item\texttt{Fonction estPleineFile(f: File) : naturel}    
    \newline
    \item\texttt{Procedure enfiler(E/S f: File, E etat: EtatDuDijkstra)}
    % Ajouter précondition
    \newline
    \item\texttt{Fonction defiler(f: File): EtatDuDijkstra}
    % Ajouter précondition
    \newline
    \item\texttt{Procedure liberer\_file(E f: File)}
\end{itemize}

\vspace{1.1em}

\textbf{Fonctions et procédures du graphe et de Dijkstra}
\begin{itemize}
    \item\texttt{Procedure recuperationInfoFichier(E fileNameVille: ChaineDeCaractères, S l, nb\_liaisons, nbCasesObligatoires, caseInitRObot: NaturelNonNul, tabCasesObligatoires: tableau[1..MAX\_CASES\_OBLIGATOIRES], tabPourGraphe[NB\_POINTS\_MAX][2], \\orientationInitRobot: tDirection): File}
    \newline
    \item\texttt{Fonction creationGrapheVille(tabLiasonsVille: tableau[1..NB\_POINTS\_MAX][2] de NaturelNonNul, nbLiaisons: NaturelNonNul) : G\_Graphe}
    \newline
    \item\texttt{Fonction dijkstra\_avec\_exclusions(g: G\_Graphe, depart, arrivee, nb\_a\_eviter : NaturelNonNul, sommet\_a\_eviter : tableau[1..NB\_POINTS\_MAX] de NaturelNonNul) : Chemin}    
    \newline
    \item\texttt{Procedure permutation(E arr: tableau[3] de NaturelNonNul, S permutations[6][3])}
    \newline
    \item\texttt{Fonction chemin\_autorise(c: Chemin): }
    \newline
    \item\texttt{Fonction verifier\_jonction\_segment(segment1, segment2: Chemin): Naturel}
    \newline
    \item\texttt{Fonction resoudre\_chemin\_plus\_court(g: G\_Graphe, depart, longueur\_ville: NaturelNonNul, directionInitialeRobot: tDirection, point\_obligatoires: tableau[3] de NaturelNonNul)}
\end{itemize}

\vspace{1.1em}

\textbf{Fonctions et procédures du fichier d'ordres}
\begin{itemize}

    \item\texttt{Fonction ordreDuChangementDeDirection(direction, nlleDirection: tDirection)}
    \newline
    \item\texttt{Procedure determinerOrdre(E/S robot: Robot, E caseSuivanteRobot, largeurCircuit: NaturelNonNul, S ordre1, ordre2: Ordre)}
    \newline
    \item\texttt{Procedure determinerOrdres(E c: Chemin, grapheVille: G\_Graphe, robot: Robot, largeurCircuit: NaturelNonNul, S tabOrdres: tableau[NB\_POINTS\_MAX] d'Ordre)}
    \newline
    \item\texttt{Procedure creationFichierOrdres(E grapheVille: G\_graphe, plusCourtChemin: Chemin, largeurCircuit: NaturelNonNul, directionInitRobot: tDirection)}
    \newline
\end{itemize}

\newpage
\subsubsection{Conception détaillée}

\paragraph{1. Types}\mbox{} \\[5pt] 
\noindent\textbf{Type tDirection} = \{NORD, SUD, EST, OUEST \}
\medskip

\noindent\textbf{Type Ordre} = \{AV, TG, TD, NO \}
\medskip

\noindent\textbf{Type Robot} = structure
\medskip
\noindent\textbf{Champs} :
\begin{itemize}
    \item \textbf{caseRobot} : Naturel Non Nul
    \item \textbf{direction} : tDirection
\end{itemize} 

\noindent\textbf{Type Chemin} = structure
\medskip
\noindent\textbf{Champs} :
\begin{itemize}
    \item \textbf{points} tableau[1..NB\_POINTS\_MAX] de NaturelNonNul
    \item \textbf{direction} : tDirection
\end{itemize} 

\noindent\textbf{Type EtatDuDijkstra} = structure
\medskip
\noindent\textbf{Champs} :
\begin{itemize}
    \item \textbf{point\_actuel} : NaturelNonNul
    \item \textbf{point\_precedent} : NaturelNonNul
    \item \textbf{chemin} : Chemin
\end{itemize} 

\noindent\textbf{Type Solution} = structure
\medskip
\noindent\textbf{Champs} :
\begin{itemize}
    \item \textbf{ordre} : tableau[1..3] de NaturelNonNul
    \item \textbf{chemin\_complet} : Chemin
\end{itemize} 

\noindent\textbf{Type file} = structure
\medskip
\noindent\textbf{Champs} :
\begin{itemize}
    \item \textbf{elemnts} : tableau[1..MAX\_FILE] de EtatDuDijkstra
    \item \textbf{debut} : NaturelNonNul
    \item \textbf{fin} : NaturelNonNul
\end{itemize}

\begin{algorithm}[H]
\DontPrintSemicolon
\SetAlgoNlRelativeSize{0}
\SetNlSty{textbf}{}{}
\caption{resoudreCheminPlusCourt (partie 1/2)}
\SetKw{KwIn}{Entrée}
\SetKw{KwOut}{Sortie}
\SetKw{KwPre}{Précondition}
\SetKw{KwRet}{Retourner}
\SetKwBlock{KwCod}{Début}{Fin}
\SetKw{KwTo}{à}
\SetKwFor{KwPour}{Pour}{faire}
\KwIn{$g$ : Graphe (G\_Graphe), $point\_obligatoires$ : tableau[3] de NNN, $depart$ : NNN, $longueur\_ville$ : NNN, $directionInitialeRobot$ : tDirection}\;
\KwOut{$solution$ : Solution}\;
\KwCod{
$solution \gets \{0\}$\; \\
$solution.chemin\_complet.nb\_points \gets 0$\; \\
$meilleure\_distance \gets 99999$\; \\
$point\_cles[0] \gets depart$\; \\
\Pour{$i \gets 1$ \KwTo $3$}{
$point\_cles[i] \gets point\_obligatoires[i-1]$\;
}
$indices\_classement \gets [1, 2, 3]$\; \\
$permutations \gets \text{permutation}(indices\_classement)$\;
\Pour{$p \gets 1$ \KwTo $6$}{
$parcours \gets [0, permutations[p][0], permutations[p][1], permutations[p][2], 0]$\; \\
$distance\_totale \gets 0$\; \\
$estValide \gets 1$\; \\
$segments \gets \text{tableau de 4 Chemin}$\;
\Pour{$i \gets 1$ \KwTo $4$}{
$index\_debut \gets parcours[i]$\; \\
$index\_fin \gets parcours[i+1]$\; \\
$sommet\_depart \gets point\_cles[index\_debut]$\; \\
$sommet\_arrivee \gets point\_cles[index\_fin]$\;
\eSi{$i > 0$}{
$sommets\_a\_eviter \gets segments[i-1].points[segments[i-1].nb\_points - 2]$\;
$nb\_a\_eviter \gets 1\;
}{
\eSi{$directionInitialeRobot = SUD$}{
$sommets\_a\_eviter \gets depart - longueur\_ville$\;
}
\eSi{$directionInitialeRobot = NORD$}{
$sommets\_a\_eviter \gets depart + longueur\_ville$\;
}
\eSi{$directionInitialeRobot = EST$}{
$sommets\_a\_eviter \gets depart - 1$\;
}
\Sinon{
$sommets\_a\_eviter \gets depart + 1$\;
}
}
$segments[i] \gets \text{dijkstra\_avec\_exclusions}(g, sommet\_depart, sommet\_arrivee, sommets\_a\_eviter, 1)$\;
}
\end{algorithm}

\begin{algorithm}[H]
\DontPrintSemicolon
\SetAlgoNlRelativeSize{0}
\SetNlSty{textbf}{}{}
\caption{resoudreCheminPlusCourt (partie 2/2)}
\SetKw{KwRet}{Retourner}
\SetKwBlock{KwSuite}{Suite}{Fin}
\SetKw{KwTo}{à}
\SetKwFor{KwPour}{Pour}{faire}

\KwSuite{
\Si{$segments[i].nb\_points = 0$}{
$estValide \gets 0$\; \\
\textbf{interrompre}\;
}
\Si{$\lnot \text{chemin\_autorise}(segments[i])$}{
$estValide \gets 0$\; \\
\textbf{interrompre}\;
}
$distance\_totale \gets distance\_totale + (segments[i].nb\_points - 1)$\;
}
\Si{$estValide$}{
\Pour{$i \gets 0$ \KwTo $2$}{
\Si{$\lnot \text{verifier\_jonction\_segments}(segments[i], segments[i+1])$}{
$estValide \gets 0$\; \\
\textbf{interrompre}\;
}
}
}
\Si{$\lnot estValide$}{
\textbf{passer à l'itération suivante}\;
}
\Si{$distance\_totale < meilleure\_distance$}{
$meilleure\_distance \gets distance\_totale$\;
\Pour{$i \gets 0$ \KwTo $2$}{
$solution.ordre[i] \gets point\_cles[permutations[p][i]]$\;
}
$index \gets 0$\;
\Pour{$i \gets 0$ \KwTo $3$}{
$debut \gets (i = 0) ? 0 : 1$\;
\Pour{$j \gets debut$ \KwTo $segments[i].nb\_points - 1$}{
\Si{$index < NB\_POINTS\_MAX$}{
$solution.chemin\_complet.points[index] \gets segments[i].points[j]$\; \\
$index \gets index + 1$\;
}
}
}
$solution.chemin\_complet.nb\_points \gets index$\;
}
}
\KwRet{solution}\;
}
\end{algorithm}

\begin{algorithm}[H]
\caption{dijkstra\_avec\_exclusion (partie 1/2)}
\SetKwInOut{Entree}{Entrée}
\SetKwInOut{Sortie}{Sortie}
\SetKwBlock{Debut}{Début}{}
\SetKw{KwRet}{Retourner}

\Entree{$g : G\_Graphe*$, $depart : $, $arrivee : $, \\
\hspace{2.5mm} $sommets\_a\_eviter : $ NNN*, $nb\_a\_eviter : $ tableau[1.. NB\_POINTS\_MAX] de NNN}
\Sortie{$resultat : Chemin$}

\Debut{
    $resultat : Chemin$\;
    $resultat$ \gets $\{0\}$\;
    $points\_visites : $ tableau$[NB\_POINTS\_MAX][NB\_POINTS\_MAX] de NNN $\;
    
    \Pour{$i$ \gets $1$ \KwTo $NB\_POINTS\_MAX$}{
        \Pour{$j$ \gets $1$ \KwTo $NB\_POINTS\_MAX$}{
            $points\_visites[i][j]$ \gets $0$\;
        }
    }
    
    $file : File$\;
    
    $file$ \gets allouer$(sizeof(File))$\;
    
    initialisation\_file$(file)$\;
    
    $initial : EtatDuDijkstra$\;
    
    $initial.point\_actuel$ \gets $depart$\;
    
    $initial.point\_precedent$ \gets $0$\;
    
    $initial.chemin.points[0]$ \gets $depart$\;
    
    $initial.chemin.nb\_points$ \gets 1\;
    
    enfiler$(file, initial)$\;
    
    $points\_visites[depart][0]$ \gets $1$\;
    
    \Tq{la file n'est pas vide}{
        $etat : EtatDuDijkstra$\;
        
        $etat \gets$ defiler$(file)$\;
        \Si{$etat.point\_actuel$ = $arrivee$}{
            $resultat.nb\_points \gets etat.chemin.nb\_points$\;
            
            \Pour{$i \gets 1$ \KwTo $etat.chemin.nb\_points$}{
                $resultat.points[i] \gets etat.chemin.points[i]$\;
            }
            
            liberer\_file$(file)$\;
            \KwRet $resultat$\;
            
        }
        
        $voisins : LCL\_Liste$\;
        
        $voisins \gets $G\_obtenirSommetsAdjacents$(g, etat.point\_actuel)$\;
        
        \Pour{$i \gets 1$ \KwTo LCL\_longueur$(voisins)$}{
        
            $voisin : NNN$\;
            
            $voisin \gets $ LCL\_element$(voisins, i)$\;
            
            \Si{$voisin$ = $etat.point\_precedent$}{
                \Continue\;
            }
            
            $est\_a\_eviter : 0$\;
            
            \Pour{$k \gets 1$ \KwTo $nb\_a\_eviter$}{
                \Si{$voisin$ = $sommet\_a\_eviter[k]$}{
                    $est\_a\_eviter \gets 1$\;
                    \Break
                }
                
            }
            
            \Si{$est\_a\_eviter$}{
                \Continue
            }
}
\end{algorithm}

\begin{algorithm}[H]
\caption{dijkstra\_avec\_exclusion (partie 2/2)}
\SetKwBlock{Suite}{Suite}{}
\SetKw{KwRet}{Retourner}

\Suite{
            $deja\_dans\_chemin \gets 0$\;
            \Si {$voisin$ <> $arrivee$}{
                \Pour{$k \gets 1$ \KwTo $etat.chemin.nb\_points$}{
                    \Si {$etat.chemin.points[k]$ = $voisin$}{
                        $deja\_dans\_chemin = 1$\;
                        \Break\;
                    }
                }
            }
            
            \Si{$deja\_dans\_chemin$}{
                \Continue\;
            }
            
            $index\_precedent : NNN$
            
            \Si{$etat.point\_actuel$ = $-1$}{
                $index\_precedent \gets 0$\;
            }
            \Sinon{
                $index\_precedent \gets etat.point\_actuel\;$
            }
            
            \Si{$points\_visites[voisin][index\_precedent]$ = $1$}{
                \Continue\;
            }
            
            $points\_visites[voisin][index\_precedent]$ \gets $1$\;
            
            $nouvel\_etat : EtatDuDijkstra$\;
            
            $nouvel_etat.point_actuel$ \gets $voisin$\;
            
            $nouvel_etat.point_precedent$ \gets $etat.point_actuel$\;
            
            $nouvel_etat.chemin.nb_points$ \gets $etat.chemin.nb_points + 1$\;
            
            \Pour{$j$ \gets $1$ \KwTo $etat.chemin.nb\_points$}{
                $nouvel\_etat.chemin.points[j] $ \gets $etat.chemin.points[j]$\;
            }
            
            $nouvel\_etat.chemin.points[etat.chemin.nb\_points]$ \gets $voisin$\;
            
            enfiler$(file, nouvel\_etat)$\;
        }
        
        LCL_vider$(voisins)$\;
        
    }
    
    liberer\_file$(file)$\;
    
    \KwRet{$resultat$}\;    
}
\end{algorithm}

\begin{algorithm}[H]
\caption{determinerOrdres}
\SetKwInOut{Entree}{Entrée}
\SetKwInOut{Sortie}{Sortie}
\SetKwBlock{Debut}{Début}{}
\SetKw{KwRet}{Retourner}

\Entree{ $c:$ Chemin, $grapheVille: $ Graphe , $robot: $ Robot, $largeurCircuit: $ NNN}
\Sortie{$tabOrdre :$ Tableau[1..NB\_POINTS\_MAX] d'ordre}
\textbf{Déclaration} $j, nbCaseChemin: $ NNN, $ordre1, ordre2: $ Ordre, \\ \hspace{2.5mm} $sommetsAdjacents: $ LCL\_Liste,\\
\hspace{2.5mm} $plusCourtChemin: $ Tableau[1..NB\_POINT\_MAX] de NNN \\ \hspace{2.5mm} 
\Debut{
    obtenirChemin$(c, plusCourtChemin)$\;
    
    $nbCaseChemin$ \gets obtenirNbPoints$(c)$\;
    
    $j$ \gets 0\;

    \Pour{$i \gets  0$ \KwTo  $nbCaseChemin$ - 1} {
    
        $sommetsAdjacents$ \gets G_obtenirSommetsAdjacents$(grapheVille, $getCaseRobot$(robot))$\;
        
        determinerOrdre$(robot, plusCourtChemin[i + 1], largeurCircuit, ordre1, ordre2)$\;
        
        
        \Si{$ordre1$ == NO} {
        
            \Si{$j$ >= 1 et $tabOrdres[j-1]$ == AV et LCL\_longueur(sommetsAdjacents) > 2} {
            
                $tabOrdres[j]$ \gets $ordre2$\;
                
                $j$ \gets $j$ + 1\;
                
            }
            
            \SinonSi{$j == 0$} {
            
                $tabOrdres[j]$ \gets $ordre2$\;
                
                $j$ \gets $j$ + 1\;
                
            }
            
        }
        
        \Sinon{
        
            $tabOrdres[j]$ \gets $ordre1$\;
            
            $j$ \gets $j$ + 1\;
            
            $tabOrdres[j]$ \gets $ordre2$\;
            
            $j$ \gets $j$ + 1 \;
            
        }
        
    }
    
    $tabOrdres[j]$ \gets NO \; 
    
    \Retour{$tabOrdre$}\;
    
}

\end{algorithm}

\begin{algorithm}[H]
\caption{determinerOrdre}
\SetKwInOut{Entree}{Entrée}
\SetKwInOut{Sortie}{Sortie}
\SetKwBlock{Debut}{Début}{}
\SetKw{KwRet}{Retourner}

\Entree{
    $robot : Robot$, \\
    $caseSuivanteRobot : Naturel$, \\
    $largeurCircuit : Naturel$
}
\Sortie{
    $ordre1 : Ordre$, \\
    $ordre2 : Ordre$
}

\textbf{Déclaration} \\
$caseInitRobot : Naturel$, \\
$dirRobot : Direction$, \\
$diff : Naturel$, \\
$nlleDirection : Direction$ \\

\Debut{
    $caseInitRobot \gets getCaseRobot(robot)$\;
    $dirRobot \gets getDirection(robot)$\;
    $diff \gets caseSuivanteRobot - caseInitRobot$\;
    $nlleDirection : Direction$\;

    \Si{$diff = 1$}{
        $nlleDirection \gets EST$\;
    }
    \SinonSi{$diff = -1$}{
        $nlleDirection \gets OUEST$\;
    }
    \SinonSi{$diff = largeurCircuit$}{
        $nlleDirection \gets SUD$\;
    }
    \SinonSi{$diff = -largeurCircuit$}{
        $nlleDirection \gets NORD$\;
    }
    \Sinon{
        $nlleDirection \gets dirRobot$\;
    }

    $ordre1 \gets ordreDuChangementDeDirection(dirRobot, nlleDirection)$\;
    $setDirection(robot, nlleDirection)$\;
    $ordre2 \gets AV$\;
    $setCaseRobot(robot, caseSuivanteRobot)$\;
}
\end{algorithm}

\subsubsection{Code C}

Maintenant que nous avons passé l'étape de la conception détaillée, nous pouvons desormais passer au développement des algorithmes en C.

Voici donc comment nous avons décider de développer les algorithmes présentés lors de la conception détaillée en C.

\paragraph{1. resoudre\_Chemin\_Plus\_Court} \mbox{} \\[5pt]

La fonction \texttt{resoudre\_chemin\_plus\_court} permet de calculer le chemin optimal d'un point de départ à lui-même en passant par trois points obligatoire et sans demi-tours:

\begin{lstlisting}[caption={Fonction de résolution du chemin optimal}, label=lst:resoudre]
Solution resoudre_chemin_plus_court(G_Graphe* g, unsigned int point_obligatoires[3], unsigned int depart, unsigned int longueur_ville, tDirection directionInitialeRobot) {

    Solution solution = {0};
    solution.chemin_complet.nb_points = 0;
    unsigned int meilleure_distance = 99999;

    unsigned int point_cles[4];
    point_cles[0] = depart;
    for(unsigned int i = 1; i < 4; i++) {
        point_cles[i] = point_obligatoires[i - 1];
    }

    unsigned int indices_classement[3] = {1, 2, 3};
    unsigned int permutations[6][3];
    permutation(indices_classement, permutations);

    // Tester chaque permutation
    for (unsigned int p = 0; p < 6; p++) {        
        unsigned int parcours[5];
        parcours[0] = 0;
        parcours[1] = permutations[p][0];
        parcours[2] = permutations[p][1];
        parcours[3] = permutations[p][2];
        parcours[4] = 0;

        unsigned int distance_totale = 0;
        unsigned int estValide = 1;
        Chemin segments[4];

        // CALCUL DES SEGMENTS AVEC EXCLUSION
        for (unsigned int i = 0; i < 4; i++) {
            unsigned int index_debut = parcours[i];
            unsigned int index_fin = parcours[i + 1];
            unsigned int sommet_depart = point_cles[index_debut];
            unsigned int sommet_arrivee = point_cles[index_fin];

            // Determiner le sommet a exclure
            unsigned int sommets_a_eviter = 0;
            unsigned int nb_a_eviter = 1;

            if (i > 0) {
                Chemin segment_precedent = segments[i - 1];
                sommets_a_eviter = segment_precedent.points[segment_precedent.nb_points - 2];
                nb_a_eviter = 1;
            } else {
                if (directionInitialeRobot == SUD) {
                    sommets_a_eviter = depart - longueur_ville;
                }
                else if (directionInitialeRobot == NORD) {
                    sommets_a_eviter = depart + longueur_ville;
                }
                else if (directionInitialeRobot == EST) {
                    sommets_a_eviter = depart - 1;
                }
                else {
                    sommets_a_eviter = depart + 1;
                }
            }

            // Calculer le chemin avec exclusions
            segments[i] = dijkstra_avec_exclusions(g, sommet_depart, sommet_arrivee, 
                                                    &sommets_a_eviter, nb_a_eviter);

            if (segments[i].nb_points == 0) {
                estValide = 0;
                break;
            }

            // Verifier les demi-tours internes
            if (!chemin_autorise(segments[i])) {
                estValide = 0;
                break;
            }

            distance_totale += (segments[i].nb_points - 1);
        }

        // Verification des jonctions
        if (estValide) {
            for (unsigned int i = 0; i < 3; i++) {
                if (!verifier_jonction_segments(segments[i], segments[i + 1])) {
                    estValide = 0;
                    break;
                }
            }
        }

        if (!estValide) {
            continue;
        }

        // Si cette permutation est valide et meilleure
        if (distance_totale < meilleure_distance) {
            meilleure_distance = distance_totale;

            for(unsigned int i = 0; i < 3; i++) {
                solution.ordre[i] = point_cles[permutations[p][i]];
            }

            // Reconstruction du chemin complet
            unsigned int index = 0;
            
            for(unsigned int i = 0; i < 4; i++) {
                unsigned int debut = (i == 0) ? 0 : 1;
                
                for(unsigned int j = debut; j < segments[i].nb_points; j++) {
                    if (index < NB_POINTS_MAX) {
                        solution.chemin_complet.points[index] = segments[i].points[j];
                        index++;
                    }
                }
            }
            
            solution.chemin_complet.nb_points = index;
        } 
    }

    return solution;
}
\end{lstlisting}

\paragraph{2. dijkstra\_avec\_exclusions} \mbox{} \\[5pt]

La fonction \texttt{dijkstra\_avec\_exclusions} permet de déterminer le plus court chemin entre deux points sans passer par certains points ni sans faire de demi-tours.

\begin{lstlisting}[caption={Fonction dijkstra avec exclusions}, label=lst:dijkstra]
Chemin dijkstra_avec_exclusions(G_Graphe* g, unsigned int depart, unsigned int arrivee, unsigned int* sommets_a_eviter, unsigned int nb_a_eviter) {
    
    Chemin resultat = {0};
    
    unsigned int points_visites[NB_POINTS_MAX][NB_POINTS_MAX];
    for (unsigned int i = 0; i < NB_POINTS_MAX; i++) {
        for(unsigned int j = 0; j < NB_POINTS_MAX; j++) {
            points_visites[i][j] = 0;
        }
    }

    File* file = malloc(sizeof(File));
    initialisation_file(file);

    EtatDuDijkstra initial;
    initial.point_actuel = depart;
    initial.point_precedent = 0;
    initial.chemin.points[0] = depart;
    initial.chemin.nb_points = 1;
    enfiler(file, initial);
    
    points_visites[depart][0] = 1;

    while (!estVideFile(file)) {
        EtatDuDijkstra etat = defiler(file);
        
        if (etat.point_actuel == arrivee) {
            resultat.nb_points = etat.chemin.nb_points;
            for (unsigned int i = 0; i < etat.chemin.nb_points; i++) {
                resultat.points[i] = etat.chemin.points[i];
            }
            liberer_file(file);
            return resultat;
        }
        
        LCL_Liste voisins = G_obtenirSommetsAdjacents(*g, etat.point_actuel);
        
        for (unsigned int i = 0; i < LCL_longueur(voisins); i++) {
            unsigned int voisin = *(unsigned int *)LCL_element(voisins, i);
            
            // Éviter le demi-tour immédiat
            if (voisin == etat.point_precedent) {
                continue;
            }
            
            // Vérifier si ce voisin est un le point à exclure
            int est_a_eviter = 0;
            for (unsigned int k = 0; k < nb_a_eviter; k++) {
                if (voisin == sommets_a_eviter[k]) {
                    est_a_eviter = 1;
                    break;
                }
            }
            
            if (est_a_eviter) {
                continue;
            }
            
            // Éviter de revisiter un point déjà dans le chemin actuel
            int deja_dans_chemin = 0;
            if (voisin != arrivee) {
                for (unsigned int k = 0; k < etat.chemin.nb_points; k++) {
                    if (etat.chemin.points[k] == voisin) {
                        deja_dans_chemin = 1;
                        break;
                    }
                }
            }
            
            if (deja_dans_chemin) {
                continue;
            }
            
            unsigned int index_precedent = (etat.point_actuel == (unsigned int)-1) ? 0 : etat.point_actuel;
            
            if (points_visites[voisin][index_precedent]) {
                continue;
            }
            
            points_visites[voisin][index_precedent] = 1;
            
            EtatDuDijkstra nouvel_etat;
            nouvel_etat.point_actuel = voisin;
            nouvel_etat.point_precedent = etat.point_actuel;
            nouvel_etat.chemin.nb_points = etat.chemin.nb_points + 1;
            
            for (unsigned int j = 0; j < etat.chemin.nb_points; j++) {
                nouvel_etat.chemin.points[j] = etat.chemin.points[j];
            }
            nouvel_etat.chemin.points[etat.chemin.nb_points] = voisin;
            
            enfiler(file, nouvel_etat);
        }
        LCL_vider(&voisins);
    }
    
    liberer_file(file);
    return resultat;
}
\end{lstlisting}

\paragraph{3. determiner\_ordres} \mbox{} \\[5pt]

La fonction \texttt{determiner\_ordres} permet de déterminer, à partir d'un chemin, l'ensemble des ordres que doit suivre le robot afin de faire le parcours demandé.

\begin{lstlisting}[caption={Fonction déterminer ordres}, label=lst:det_ordres]
void determinerOrdres(Ordre tabOrdres[NB_POINTS_MAX], Chemin c, G_Graphe grapheVille , Robot robot, unsigned int largeurCircuit) {
    
    Ordre ordre1, ordre2;
    unsigned int plusCourtChemin[NB_POINTS_MAX];
    obtenirChemin(c, plusCourtChemin);
    unsigned int nbCaseChemin = obtenirNbPoints(c);
    unsigned int j = 0;

    for (unsigned int i = 0; i < nbCaseChemin - 1; i++) {
        LCL_Liste sommetsAdjacents = G_obtenirSommetsAdjacents(grapheVille, getCaseRobot(robot));
        determinerOrdre(&robot, plusCourtChemin[i + 1], largeurCircuit, &ordre1, &ordre2);
        if (ordre1 == NO) {
            if (j >= 1 && tabOrdres[j-1] == AV && LCL_longueur(sommetsAdjacents) > 2) {
                tabOrdres[j] = ordre2;
                j = j + 1;
            }
            else if (j == 0) {
                tabOrdres[j] = ordre2;
                j = j + 1;
            }
        }
        else {
            tabOrdres[j] = ordre1;
            j = j + 1;
            tabOrdres[j] = ordre2;
            j = j + 1;
        }
    }
    tabOrdres[j] = NO; // Marqueur de fin des ordres
}
\end{lstlisting}

paragraph{4. determiner\_ordre} \mbox{} \\[5pt]

La fonction \texttt{determiner\_ordre} permet de déterminer, les ordres que doit suivre le robot afin d'aller d'un point à un autre en fonction de sa direction.

\begin{lstlisting}[caption={Fonction déterminer ordre}, label=lst:det_ordre]
void determinerOrdre(Robot* robot, unsigned int caseSuivanteRobot, unsigned int largeurCircuit, Ordre* ordre1, Ordre* ordre2) {
    
unsigned int caseInitRobot = getCaseRobot(*robot);
    tDirection dirRobot = getDirection(*robot);
    unsigned int diff = caseSuivanteRobot - caseInitRobot;
    tDirection nlleDirection;

    if (diff == 1) {  // Le switch a été remplacer par des ifs car on ne peux mettre de valeurs inconnu a la compilations en label 
        nlleDirection = EST;
    }
    else if (diff == -1) {
        nlleDirection = OUEST;
    }
    else if (diff == largeurCircuit) {
        nlleDirection = SUD;
    }
    else if (diff == -largeurCircuit) {
        nlleDirection = NORD;
    }
    else {
        nlleDirection = dirRobot; // Pas de changement de direction
    }
    *ordre1 = ordreDuChangementDeDirection(dirRobot, nlleDirection);
    setDirection(robot, nlleDirection);
    *ordre2 = AV;
    setCaseRobot(robot, caseSuivanteRobot);    
}
\end{lstlisting}


La fonction \texttt{resoudre\_chemin\_plus\_court} permet de calculer le chemin optimal en testant toutes les permutations possibles :

\begin{lstlisting}[caption={Fonction de résolution du chemin optimal}, label=lst:resoudre]
Solution resoudre_chemin_plus_court(G_Graphe* g, unsigned int point_obligatoires[3], 
                                     unsigned int depart, unsigned int longueur_ville, 
                                     tDirection directionInitialeRobot) {
    Solution solution = {0};
    solution.chemin_complet.nb_points = 0;
    unsigned int meilleure_distance = 99999;

    unsigned int point_cles[4];
    point_cles[0] = depart;
    for(unsigned int i = 1; i < 4; i++) {
        point_cles[i] = point_obligatoires[i - 1];
    }

    unsigned int indices_classement[3] = {1, 2, 3};
    unsigned int permutations[6][3];
    permutation(indices_classement, permutations);

    // Tester chaque permutation
    for (unsigned int p = 0; p < 6; p++) {        
        unsigned int parcours[5];
        parcours[0] = 0;
        parcours[1] = permutations[p][0];
        parcours[2] = permutations[p][1];
        parcours[3] = permutations[p][2];
        parcours[4] = 0;

        unsigned int distance_totale = 0;
        unsigned int estValide = 1;
        Chemin segments[4];

        // CALCUL DES SEGMENTS AVEC EXCLUSION
        for (unsigned int i = 0; i < 4; i++) {
            unsigned int index_debut = parcours[i];
            unsigned int index_fin = parcours[i + 1];
            unsigned int sommet_depart = point_cles[index_debut];
            unsigned int sommet_arrivee = point_cles[index_fin];

            // Determiner le sommet a exclure
            unsigned int sommets_a_eviter = 0;
            unsigned int nb_a_eviter = 1;

            if (i > 0) {
                Chemin segment_precedent = segments[i - 1];
                sommets_a_eviter = segment_precedent.points[segment_precedent.nb_points - 2];
                nb_a_eviter = 1;
            } else {
                if (directionInitialeRobot == SUD) {
                    sommets_a_eviter = depart - longueur_ville;
                }
                else if (directionInitialeRobot == NORD) {
                    sommets_a_eviter = depart + longueur_ville;
                }
                else if (directionInitialeRobot == EST) {
                    sommets_a_eviter = depart - 1;
                }
                else {
                    sommets_a_eviter = depart + 1;
                }
            }

            // Calculer le chemin avec exclusions
            segments[i] = dijkstra_avec_exclusions(g, sommet_depart, sommet_arrivee, 
                                                    &sommets_a_eviter, nb_a_eviter);

            if (segments[i].nb_points == 0) {
                estValide = 0;
                break;
            }

            // Verifier les demi-tours internes
            if (!chemin_autorise(segments[i])) {
                estValide = 0;
                break;
            }

            distance_totale += (segments[i].nb_points - 1);
        }

        // Verification des jonctions
        if (estValide) {
            for (unsigned int i = 0; i < 3; i++) {
                if (!verifier_jonction_segments(segments[i], segments[i + 1])) {
                    estValide = 0;
                    break;
                }
            }
        }

        if (!estValide) {
            continue;
        }

        // Si cette permutation est valide et meilleure
        if (distance_totale < meilleure_distance) {
            meilleure_distance = distance_totale;

            for(unsigned int i = 0; i < 3; i++) {
                solution.ordre[i] = point_cles[permutations[p][i]];
            }

            // Reconstruction du chemin complet
            unsigned int index = 0;
            
            for(unsigned int i = 0; i < 4; i++) {
                unsigned int debut = (i == 0) ? 0 : 1;
                
                for(unsigned int j = debut; j < segments[i].nb_points; j++) {
                    if (index < NB_POINTS_MAX) {
                        solution.chemin_complet.points[index] = segments[i].points[j];
                        index++;
                    }
                }
            }
            
            solution.chemin_complet.nb_points = index;
        } 
    }

    return solution;
}
\end{lstlisting}

\subsection{Conclusion}
\subsubsection{Répartition du travail}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.5}
\small
\begin{tabularx}{\textwidth}{|X|c|c|c|c|c|c|}
\hline
\textbf{Tâches} & \textbf{Jahnis} & \textbf{Mathieu} & \textbf{Alioune} & \textbf{Ylann} & \textbf{Ilan} & \textbf{Neil}\\ \hline
Construction du robot & X & X & X & X & X & X \\ \hline
Fonctions moteur & X & X & X &  & X &  \\ \hline
Fonctions capteurs &  & X &  &  & X & X \\ \hline
Fonctions LCD &  & X & X & X & X &  \\ \hline
Tests unitaires électronique & X & X &  &  & X &  \\ \hline
Débogage électronique &  & X &  &  & X & \\ \hline
Fonctions liées à la determination du plus court chemin & X &  &  &  &  &  \\ \hline
Fonctions liées à la création du graphe &  &  &  &  &  & X \\ \hline
Tests unitaires algorithme & X &  &  &  &  & X \\ \hline
Débogage algorithme & X &  &  &  &  & X \\ \hline
Mise en place du makefile et bashrc &  &  &  &  &  & X \\ \hline
Rédation du rapport & X & X & X & X & X & X \\ \hline
\end{tabularx}
\caption{Répartition des tâches}
\end{table}

\subsubsection{Conclusion du projet}

En conclusion, ce projet nous a permis de mettre nos connaissances en pratique afin de développer un robot autonome capable se déplacer selon un parcours donné.
Les choix des composants ainsi que le choix de l'algorithme pour trouver le plus court chemin nous a permis d'atteindre un résultat plutôt satisfaisant en terminant deuxième du classement lors de la soutenance.
Pendant le projet, nous avons fait face à de nombreux défis et de nombreuses difficultés comme la gestion du capteur RGB pour la detection des pastilles des couleurs.
Toutefois, nous avons su communiquer et nous entraider pour surmonter ces difficultés et améliorer du mieux que l'on pouvait notre robot.

Un point très important que nous souhaitons évoquer: les différences entre la théorie et la réalité. 
Effectivement, nous avions dès le début défini le nombre de composant que nous souhaitions utilisés ainsi que la manière dont nous souhaitions réaliser les choses.
Néanmoins, nous avons été obligés de faire de nombreux ajustements après plusieurs tests, tant dans la partie éléctrique du robot, que dans la partie résolution du parcours.

Finalement, ce projet nous a appris à travailler sur plusieurs aspects différents d'un même prochet en répartissant le travail selon les préférences et forces de chacun.
L'utilisation de Git a d'ailleurs été indispensable pour coodonner ce projet efficacement.
Au terme de cette experience, nous avons pu nous préparer aux attentes des entreprises et ce, malgré le delais assez court. 
Elle nous a permis de découvrir des méthodes de travail efficace et d'appliquer concrètement nos compétences théoriques.


\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{team_megatron.JPG}
    \caption{Groupe 15 du PI-smart robot}
    \label{fig:monimage}
\end{figure}

\subsubsection{Commit Git}

Voici le nombre de commit des membres du groupe :\\
\begin{itemize}
    \item\texttt{Ilan et Mathieu : 40 commit environ (travail en duo sur l'electronique)}

    \item\texttt{Jahnis : 55 commit environ}
    
    \item\texttt{Neil : 40 commit environ}    

    \item\texttt{Alioune et Ylann : 15 commit environ (assistance  la résolution de prolbèmes}
    
    \item\texttt{Fonction defiler(f: File): EtatDuDijkstra}
    
    \item\texttt{Procedure liberer\_file(E f: File)}
\end{itemize}

\section{Bibliographie}
\begin{thebibliography}{9}

\bibitem{tcs34725}
Adafruit, \textit{TCS34725 Color Sensor Datasheet}.  
\url{https://cdn-shop.adafruit.com/datasheets/TCS34725.pdf}

\bibitem{tcrt5000}
Powertech, \textit{Capteur de suivi de ligne infrarouge TCRT5000}.  
\url{https://powertech-dz.org/product/capteur-de-suivi-de-ligne-infrarouge-tcrt5000-ir-1-4-5-8-canaux/}

\bibitem{l293d}
Texas Instruments, \textit{L293D Motor Driver Datasheet}.  
\url{https://moodle.insa-rouen.fr/pluginfile.php/191118/mod_resource/content/3/l293d.pdf}

\bibitem{lcd1602}
Adafruit, \textit{LCD1602 Datasheet}.  
\url{https://cdn-shop.adafruit.com/datasheets/TC1602A-01T.pdf}

\bibitem{buzzer}
Valeurs moyennes issues de plusieurs sources en ligne (absence de documentation officielle).

\bibitem{pcf8574}
Philips, \textit{PCF8574 I/O Expander Datasheet}.  
\url{https://www.alldatasheet.fr/datasheet-pdf/view/424340/PHILIPS/PCF8574T.html}

\bibitem{raspberrypi}
Raspberry Pi Foundation, \textit{Raspberry Pi 4 Datasheet}.  
\url{https://datasheets.raspberrypi.com/rpi4/raspberry-pi-4-datasheet.pdf}

\end{thebibliography}


\end{document}

